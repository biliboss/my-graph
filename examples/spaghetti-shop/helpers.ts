//! helpers — o irmão gêmeo do utils. Ninguém sabe a diferença.
// THIS ONE IS NOT A DRAFT.
import type { Utils } from "./utils";
import type { Common } from "./common";
export interface Helpers {
	normalize(): void;
	guess(): void;
}
