//! misc — o nome já confessa.
// THIS ONE IS NOT A DRAFT.
import type { Helpers } from "./helpers";
import type { Legacy } from "./legacy";
export interface Misc {
	banner(): void;
	popup(): void;
}
